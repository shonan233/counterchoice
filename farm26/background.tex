\section{Background}
\label{sec:background}

\subsection{Counterpoint}

% \begin{figure*}
%   \includegraphics[width=16cm]{rules.png}
%   \vspace{-6mm}
%   \caption{Key Rules of First-Species Counterpoint}
%   \label{fig:rules}
% \end{figure*}

Counterpoint is an approach in western classical music for composing music with
multiple voices.
Species counterpoint is a method for learning how to compose
counterpoint, and first-species counterpoint is the simplest variant
consisting of two melodies sharing the same rhythm.
To compose counterpoint, one starts with a pre-existing melody called the
\emph{cantus firmus}, and writes a counterpoint line either above or below the
cantus firmus.

Here is an example of first-species counterpoint composed against a
phrase from the German folk song \emph{Froschgesang}.

\begin{center}
\includegraphics[width=7cm]{goodcp.png}
\end{center}

There are various sets of rules associated to counterpoint intended to ensure
that the two melodies sound pleasing both when played together and when played individually. 
For example, the following are three key rules of first-species counterpoint as articulated by Fux~\cite{fux-cp}.

\begin{itemize}
  \item The interval between the two notes in a bar must be consonant.
  \item The two voices should not move in the same direction into a fifth or an octave.
  \item The last interval must be a unison or an octave reached by stepwise contrary motion.
\end{itemize}

% \begin{itemize}
%   \item \textbf{End with a cadence.}
%     The final pair of notes must form a unison or an octave, and this interval
%     must be approached by stepwise contrary motion (Figure~\ref{fig:rules} left).
%     This progression is called a cadence, and creates a sense of resolution.
%   
%   \item \textbf{No direct fifths or octaves.}
%     It is forbidden for the two voices to form a fifth or an octave by movement
%     in the same direction (Figure~\ref{fig:rules} middle). This forbidden
%     progression is called a direct fifth or octave, and it destroys the
%     independence of the voices.
%   
%   \item \textbf{No successive large leaps in the same direction.}
%     The counterpoint line should not leap multiple times in the same direction
%     by large intervals, namely fifths or sixths (Figure~\ref{fig:rules} right).
%     Such a motion disrupts the flow of the melody.
% \end{itemize}

The example we saw above adheres to these rules, and it indeed sounds
good to human ears.
In contrast, the example below breaks these rules, and hence it does
not sound as good as the first example.
Specifically, the parallel fifths in bars 2 and 3 break the
independence of the melodies, the dissonant intervals in bars 4 and
5 create an unstable feeling, and the parallel thirds in bars 6 and 7
fail to establish a sense of resolution.
Interested readers may compare the two examples by playing the audio
files at \url{https://shonan233.github.io/counterchoice/audio_examples/}.

\begin{center}
\includegraphics[width=7cm]{badcp.png}
\end{center}

Our Dusa encoding of first-species counterpoint
(Section~\ref{sec:dusa-implementation}) enforces these and a handful of other
rules.

\subsection{Finite-Choice Logic Programming in Dusa}

Finite-choice logic programming~\cite{martens2025finite} (FCLP) is a new
foundation for writing logic programs that combine deductive specification
and mutual-exclusion constraints. Unlike more established logic programming
languages like Datalog and Prolog, whose meaning is a single canonical
(smallest) model justified by the rules, the meaning of an FCLP program is
the full collection of models consistent with the theory represented by the
program.  This paradigm is similar to answer set
programming~\cite{lifschitz2019answer}, but unlike ASP, it is designed to support
forward-chaining operational semantics (similar to Datalog) that
incrementally enumerates solutions (and thus more naturally represents
infinite collections of models). 
Dusa\footnote{\url{https://github.com/robsimmons/dusa}} is FCLP's reference
implementation, which stochastically explores the search space and emits
solutions lazily. The combination of (1) declarative representation via
relations (shared with Prolog and Datalog), (2) mutual-exclusion
constraints that give rise to multiple valid models (shared with ASP), and (3)
lazy stochastic search for generating those models (unique to Dusa) 
made it an attractive choice for this application.

A Dusa program, like all logic programs, consists of a collection
of logical {\em rules} and {\em facts} that specify valid deductions
over a custom domain of {\em predicates}. Predicates in Dusa
may be associated with unique {\em values} (in which case we
sometimes refer to them as {\em attributes}). For example, in
the domain of counterpoint, we use the predicate
\verb|cp T is I| to mean ``the counterpoint voice
at time step \verb|T| has interval \verb|I|.''
The \verb|is| keyword represents a uniqueness constraint, i.e. that for any
time point \verb|T|, there can only be one \verb|I| such that \verb|cp T is I|.

A {\em rule} is a schema for deduction that may use {\em logic variables}
(written beginning with capital letters, by convention) that are implicitly
universally quantified over the scope of the whole rule. Here is an
example (not an actual rule in our system):

\begin{verbatim}
  cp T is I :- cf T is N, plus N 2 is I.
\end{verbatim}

One can read the `\verb|:-|' symbol as a ``backwards implication''
and the comma `\verb|,|' as conjunction. Thus,
this rule says that whenever the cantus firmus at time step \verb|T| is the
note \verb|N|, and the sum of \verb|N| and $2$ is \verb|I|,
it {\em must} be the case that the counterpoint at time step
\verb|T| is \verb|I|. The comma-separated predicates to the right of the
`\verb|:-|' symbol are the {\em premises} of the rule, and the
single predicate to its left is the {\em head} of the rule.

A {\em fact} is simply a rule with no premises. In Dusa,
every logic variable appearing in the head of a rule must
occur in its premises, so facts must mention only {\em ground terms}, those
that do not contain any logic variables. For example, this fact
asserts that the counterpoint interval at time step $0$ is a fifth:

\begin{verbatim}
  cp 0 is fifth.
\end{verbatim}

\subsubsection{Choice Rules}

The main distinguishing feature of finite-choice logic
programming is the use of {\em choices} in rules, which
describe a set of mutually-exclusive assignments from
attributes to values. For example, the following rule
defines a predicate \verb|my_interval| which can take on
any of \verb|third|, \verb|fifth|, or \verb|unison|
as values:

\begin{verbatim}
  my_interval is {third, fifth, unison}.
\end{verbatim}

Choice rules introduce the plurality of models that the
solver may find, populating the {\em possibility space}
of distinct but equally valid solutions. These models
may be invalidated by other rules, however, that introduce
distinct assignments to the same predicate. For instance, if
another rule asserted that \verb|my_interval is octave|,
the program would have no valid (consistent) models.

The previous rule is an example of a {\em closed} rule,
meaning that \verb|third|, \verb|fifth|, and \verb|unison|
are the {\em only} values that \verb|my_interval| may
accept.
In addition, Dusa has {\em open} rules, notated with the
(\verb|is?|) keyword, which permit
but do not require the attribute to accept the values.
(In general, attributes need not be associated with any value,
and indeed will not be unless a rule justifies it.)
The following two rules can coexist and produce a non-empty
set of models:

\begin{verbatim}
  my_interval is? {third, fifth, unison}.
  my_interval is octave :- flag is on.
  flag is {on, off}.
\end{verbatim}

The only valid solutions would assign the interval
to be \verb|octave|. Intuitively, one may think of open
rules as describing ``defaults'' which may be overridden
by closed rules. Our code uses open rules to describe the
full set of possible intervals, which is refined
by closed rules in the presence of certain constraints
(see ``Intervals and consonance'' in Section~\ref{sec:dusa-implementation}).

\subsubsection{Syntactic Sugar}

We use three key pieces of syntactic sugar in this paper:
\begin{itemize}
  \item {\bf Using attributes like function calls.} Since
    the values of attributes are unique, we can treat them
    like functions and place their application in term
    position. For example, we can write
    \verb|interval is (plus 2 4)| instead of
    \verb|interval is X :- plus 2 4 is X|.
  \item {\bf \verb|#forbid| and \verb|#demand|.} Choice rules and
    functional dependencies are enough to represent
    constraints more naturally expressed as ``$P$ must hold in
    all valid solutions''
    and ``$Q$ must not hold in valid solutions'' for certain propositions $P,Q$
    expressible in Dusa. Dusa permits expressing these
    constraints directly as \verb|#demand| $P$ and
    \verb|#forbid| $Q$. If you are interested in how these
    constraints are elaborated, please see the Dusa documentation.\footnote{\url{https://dusa.rocks/docs/}}
  \item {\bf \verb|#lazy| predicates.} By default, Dusa executes with
    forward-chaining deduction, eagerly producing all facts that are
    justified by and consistent with the program (similar to Datalog).
    However, some definitions entail infinitely many such facts (such
    as the definition of addition), so we should not eagerly ``unroll''
    this definition as part of the generated solution.
    A \verb|#lazy| declaration for a predicate indicates that Dusa should
    expand the definition only in a goal-directed (backward-chaining)
    manner, when it is used as the premise to another rule. (This is
    syntactic sugar which is elaborated into forward-chaining
    rules by a variant on established
    techniques~\cite{bancilhon1985magic,beeri1987power,tekle2010precise}.)
\end{itemize}

\subsubsection{Executing a Dusa program}

The input to the Dusa solver is a collection of facts and
rules. The output is a nondeterministic {\em stream} of
valid models, which is complete relative to the semantics
(please see the paper~\cite{martens2025finite} for details).
Dusa programs can be written and run in the browser (hosted at the URL
~\url{https://dusa.rocks} at time of writing), and there
is a JavaScript API for creating ``solver'' instances
that can be queried asynchronously (or as a generator), which
we employ in our implementation.

Finding solutions to finite-choice logic programs
is NP-hard: it is fairly trivial to use it to express
SAT solving. In addition, Dusa is a research prototype and not finely tuned
for efficiency, unlike modern SAT solvers and answer set solvers
based on them. Expressing programs in a way that Dusa can
(relatively) efficiently find solutions is a challenge,
which we discuss in Section~\ref{sec:discussion}.

% TODO stochastic search; streaming solutions; web impl and API


